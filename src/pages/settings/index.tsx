import React from 'react'

const Settings = () => {
    return (
        <div className='h-[100vh] bg-[rgb(var(--background))] flex items-center justify-center text-[rgb(var(--text))]'>

        </div>
    )
}

export default Settings
